server {
    listen                 	0.0.0.0:80;
    server_names            hello.ma;

    location / {
        root                    ./www/static_website_root;
		index 				    index.html index.htm;
        autoindex               off;
    }
}
